all:
  children:
    managers:
      hosts:
        jenkins@swarm-master:
    workers:
      hosts:
        jenkins@swarm-worker:
        jenkins@swarm-worker2:
        
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: '~/.ssh/ansible_id_rsa'
    ansible_python_interpreter: "/usr/bin/python3"
