all:
  children:
    managers:
      hosts:
        swarm-master:
    workers:
      hosts:
        swarm-worker:
        swarm-worker2:
        
  vars:
    ansible_user: jenkins
    ansible_ssh_private_key_file: '~/.ssh/ansible_id_rsa'
